/// Glaciation mask: None / Former / Actively Glaciated.
/// Phase 5, Task P5.5.

use crate::noise::params::GlacialClass;

pub fn compute_glaciation_mask(
    _width: usize,
    _height: usize,
    _glaciation_slider: f32,
) -> Vec<GlacialClass> {
    todo!("Phase 5: implement glaciation mask (P5.5)")
}
